let canvas, context, mousePosition, isKeyPressed, isMouseDown, deltaTime, absoluteTime;
module.exports = {
    canvas,
    context,
    mousePosition,
    isKeyPressed,
    isMouseDown,
    deltaTime,
    absoluteTime
};